%table
  %tr
    %th
      Drug
    %th
      Prescription Code
    %th
      Prescribing Physician
    %th
      Dose (in mg)
    %th
      Quantity
    %th
      Refills Remaining
    %th
      Last Filled On
    %th
      Actions
  - @patient.medications.each do |medication|
    %tr
      %td
        = link_to "#{medication.drug.name}", drug_path(medication.drug)
      %td
        = medication.prescription
      %td
        = medication.prescribing
      %td
        = medication.dose
      %td
        = medication.quantity
      %td
        = medication.refills
      %td
        = medication.filled_on.strftime('%m %d, %Y')
      %td
        = link_to "Edit", edit_medication_path(medication)
        |
        = link_to "Delete", medication_path(medication), :method => 'delete', :confirm => "Are you sure you want to delete this prescription?"
      

= link_to "Add prescription information", new_medication_path(:patient_id => @patient.id)